# Public файлууд - бүх файлыг ignore хийнэ, зөвхөн .htaccess үлдээнэ
/public_html/public/*
!/public_html/public/.htaccess

# Protected хавтас (cache + protected files), зөвхөн .htaccess үлдэнэ
/protected/*
!/protected/.htaccess

# Composer vendor - автоматаар татагддаг тул оруулахгүй
/vendor/
composer.lock

# Environment variables - серверийн нууц тохиргоо (DB нууц үг, API key, JWT secret гэх мэт)
# Энэ файлд системийн нэвтрэх эрх, нууц түлхүүрүүд агуулагддаг тул git-д ОРУУЛЖ БОЛОХГҮЙ.
.env

# PHPUnit cache
.phpunit.cache/

# NetBeans private тохиргоо
/nbproject/private/

# Migration SQL файлууд - per-environment upload (file-based system).
# Зөвхөн .gitkeep ба README.md git-д үлдэнэ; бусад файлууд server-ээс
# upload хийгдэнэ.
/database/migrations/*
!/database/migrations/.gitkeep
!/database/migrations/README.md
